const { Op, Sequelize } = require('sequelize');
const { StatusCodes, ReasonPhrases } = require('http-status-codes');

const handleUserScoreUpdateForLeaderboard = async (quizId, userId, newScore,) => {

}

const sendLeaderboadUpdate = async (quizId, userId, newScore,) => {

}

const updateLeaderboard = async (quizId, userId, newScore,) => {

}

module.exports = {
    handleUserScoreUpdateForLeaderboard,
    sendLeaderboadUpdate,
    updateLeaderboard,
};
  